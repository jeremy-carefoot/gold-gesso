export default defineAppConfig({
  ui: {
    colors: {
      primary: 'main'
    },
    button: {
      slots: { base: 'cursor-pointer' }
    },
    checkbox: {
      slots: { base: 'cursor-pointer' }
    }
  }
});